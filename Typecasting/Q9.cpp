#include <bits/stdc++.h>
using namespace std;

#define MULTIPLY_BY_TWO(a){cout<<"Mul by 2: "<<(a*2)<<endl;}
#define DIVIDE_BY_TWO(a){cout<<"Div by 2: "<<(a/2)<<endl;}


int main() {
	int a;
	
	cout<<"Enter a: ";cin>>a;
	
	MULTIPLY_BY_TWO(a);
	DIVIDE_BY_TWO(a);

	return 0;
}