#include <bits/stdc++.h>
using namespace std;

class Message {
	string value;
	Message operator + (Message const &obj) { 
	Message res;
	ofstream outfile;
	outfile.open("formatted.txt");
	for (int i = 0; i < 100; ++i)
	 {
	 	infile>>value;
	 } 
	return res; 
	}	
}
